#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <stack>
#include <map>
using namespace std;

struct info
{
    string mm;
    string dd;
    string time;
    string obj;
    string name;
    int none;
    bool flag;
};

/***/
int boj()
{
    info Obj;
    map<string, info> objInfo;

    cout << objInfo["2"].flag;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    boj();
    return 0;
}