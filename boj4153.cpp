#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <stack>
using namespace std;

/*

*/

int boj()
{

    int a, b, c;

    int max;
    do
    {

        int num = 3;
        max = 0;
        cin >> a >> b >> c;

        while (num--)
        {
            
        }

    } while (a != 0 && b != 0 && c != 0);
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    boj();
    return 0;
}